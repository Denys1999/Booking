import { makeStyles } from '@material-ui/styles';

const useStylesFormSearch = makeStyles(theme=>({
  root:{
      backgroundColor: theme.palette.secondary.main,
      padding:20,
      borderRadius:50,
      boxShadow:'12px 10px 24px 4px rgba(2, 7, 11, 0.2);',
  },
}
  


)
  
   
    
);
  export default useStylesFormSearch ;